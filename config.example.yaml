database_path: /data/media-finder.db
scan_workers: 10
scan_buffer_size: 100  # Buffer size for file processing queue
api_timeout: 30s
server_port: 8080
cors_allowed_origin: "http://localhost:8080"  # Set to "*" for development
stats_cache_ttl: 30s  # How long to cache statistics

# Database connection pool settings
db_max_open_conns: 25
db_max_idle_conns: 5
db_conn_max_lifetime: 5m

# Media-finder's own container-to-host mappings
local_path_mappings:
  - container: /media
    host: /mnt/user/data/media
  - container: /downloads
    host: /mnt/user/data/downloads/torrents

# Each service's container-to-host mappings
service_path_mappings:
  plex:
    - container: /media
      host: /mnt/user/data/media
  sonarr:
    - container: /tv
      host: /mnt/user/data/media/tv
    - container: /downloads
      host: /mnt/user/data/downloads/torrents
  radarr:
    - container: /movies
      host: /mnt/user/data/media/movies
    - container: /downloads
      host: /mnt/user/data/downloads/torrents
  qbittorrent:
    - container: /downloads
      host: /mnt/user/data/downloads/torrents

services:
  plex:
    url: http://plex:32400
    token: YOUR_PLEX_TOKEN_HERE
  sonarr:
    url: http://sonarr:8989
    api_key: YOUR_SONARR_API_KEY_HERE
  radarr:
    url: http://radarr:7878
    api_key: YOUR_RADARR_API_KEY_HERE
  qbittorrent:
    url: http://qbittorrent:8080
    username: admin
    password: adminpass
    # Optional: Use qui proxy instead of direct connection
    # qui_proxy_url: http://qui:7476/proxy/YOUR_PROXY_KEY_HERE

# Paths to scan (as seen by media-finder container)
scan_paths:
  - /media
  - /downloads

